# s-xray v1.2.1 更新说明

## 更新概述

本次更新主要优化了主菜单显示效果，修复了格式显示问题，并改进了用户体验。

---

## 主要改进

### 1. 修复主菜单显示格式 ✅

**问题**: 之前使用 `printf` 格式化 (`%-23s`, `%-23d`) 导致显示不正确

**修复前**:
```
│  内核版本: %-23s│
│  运行状态: 运行中                      │
│  节点数量: %-23d│
│  用户数量: %-23d│
```

**修复后**:
```
│  内核版本: v1.8.8
│  运行状态: 运行中
│  节点数量: 3
│  用户数量: 5
```

**技术细节**:
- 移除 `printf` 格式化符号
- 直接使用变量替换 `${variable}`
- 简化显示逻辑，提高可读性

---

### 2. 优化菜单编号 🔢

**变更**: 将卸载选项从 `99` 改为 `9`，按顺序排列

**修改前**:
```
1.  内核管理
2.  节点管理
...
8.  证书管理
99. 卸载脚本  ← 编号跳跃
0.  退出脚本
```

**修改后**:
```
1.  内核管理
2.  节点管理
...
8.  证书管理
9.  卸载脚本  ← 顺序排列
0.  退出脚本
```

**优点**:
- ✅ 编号连续，符合用户习惯
- ✅ 避免误操作（不再需要输入两位数）
- ✅ 更简洁直观

---

### 3. 界面美化增强 🎨

#### 标题高亮
- "系统状态" 和 "功能菜单" 使用黄色高亮
- 提升视觉层次感

#### 版本显示
- 主菜单显示版本号 v1.2.1
- 方便用户确认脚本版本

#### 卸载界面统一
```
╔═══════════════════════════════════════╗
║          警告：卸载脚本              ║
╚═══════════════════════════════════════╝

此操作将卸载 s-xray 管理脚本
包括所有配置文件和数据
```

---

## 完整菜单预览

```
╔═══════════════════════════════════════╗
║    Xray-Core 一键管理脚本 v1.2.1    ║
╚═══════════════════════════════════════╝

┌─────────────────────────────────────┐
│  系统状态                           │
├─────────────────────────────────────┤
│  内核版本: v1.8.8
│  运行状态: 运行中
│  节点数量: 3
│  用户数量: 5
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  功能菜单                           │
├─────────────────────────────────────┤
│  1.  内核管理                       │
│  2.  节点管理                       │
│  3.  用户管理                       │
│  4.  订阅管理                       │
│  5.  防火墙管理                     │
│  6.  配置管理                       │
│  7.  域名管理                       │
│  8.  证书管理                       │
├─────────────────────────────────────┤
│  9.  卸载脚本                       │
│  0.  退出脚本                       │
└─────────────────────────────────────┘
```

---

## 代码变更摘要

### 文件: `xray-manager.sh`

#### 1. 主菜单显示函数 (行 135-164)
```bash
# 修改前
echo -e "${CYAN}│${NC}  内核版本: ${YELLOW}%-23s${CYAN}│${NC}" "$version"
echo -e "${CYAN}│${NC}  运行状态: $status                      ${CYAN}│${NC}"
echo -e "${CYAN}│${NC}  节点数量: ${BLUE}%-23d${CYAN}│${NC}" "$node_count"

# 修改后
echo -e "${CYAN}│${NC}  内核版本: ${YELLOW}${version}${NC}"
echo -e "${CYAN}│${NC}  运行状态: ${status}"
echo -e "${CYAN}│${NC}  节点数量: ${BLUE}${node_count}${NC}"
```

#### 2. 主程序逻辑 (行 407-439)
```bash
# 修改前
case $choice in
    ...
    99)  # 卸载脚本
    ...

# 修改后
case $choice in
    ...
    9)  # 卸载脚本
    ...
```

#### 3. 版本号更新
```bash
# 修改前
log_info "Xray 管理脚本启动 (v1.2.0)"

# 修改后
log_info "Xray 管理脚本启动 (v1.2.1)"
```

---

## 测试验证

### 测试脚本
创建了 `test-display.sh` 用于验证显示效果：

```bash
# 运行测试
bash test-display.sh
```

### 测试内容
1. ✅ 主菜单显示格式
2. ✅ 系统状态信息显示
3. ✅ 菜单编号顺序
4. ✅ 卸载界面显示
5. ✅ 颜色和框线效果

---

## 兼容性说明

### 完全向后兼容 ✅
- ✅ 不影响现有配置文件
- ✅ 不改变功能逻辑
- ✅ 仅优化显示界面
- ✅ 保留所有原有功能

### 用户影响
- 📌 **唯一变化**: 卸载选项从输入 `99` 改为输入 `9`
- 📌 **其他操作**: 完全不变

---

## 升级指南

### 方式 1: Git 更新（推荐）
```bash
cd /opt/s-xray
git pull
```

### 方式 2: 直接替换文件
```bash
# 备份当前脚本
cp /usr/local/bin/xray /usr/local/bin/xray.bak

# 下载新版本
wget -O /opt/s-xray/xray-manager.sh <新版本URL>

# 重新创建软链接（如果需要）
ln -sf /opt/s-xray/xray-manager.sh /usr/local/bin/xray
```

### 验证升级
```bash
# 运行脚本
xray

# 检查版本号显示
# 应该显示: Xray-Core 一键管理脚本 v1.2.1
```

---

## 问题反馈

如果遇到任何问题，请：

1. **检查版本**: 确认主菜单显示 v1.2.1
2. **查看日志**: `/var/log/xray-manager.log`
3. **提交 Issue**: 在项目仓库提交问题报告

---

## 下一步计划

### v1.3.0 规划
- [ ] 节点性能监控面板
- [ ] 流量统计可视化
- [ ] 多用户权限管理
- [ ] 订阅链接二维码生成
- [ ] 自动证书续期集成

---

## 致谢

感谢所有用户的反馈和建议！

特别感谢：
- 报告显示格式问题的用户
- 建议优化菜单编号的用户

---

**更新日期**: 2025-10-10
**版本**: v1.2.1
**维护者**: s-xray Team
