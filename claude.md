# Claude Code 项目指令

## 核心原则

### 1. 不生成非代码文件
- ❌ 不要生成报告、指南、总结、文档等非必要文件
- ❌ 不要在 `claudedocs/` 目录创建说明性文档
- ✅ 只修改和创建必要的源代码文件
- ✅ 可以在响应中口头总结，但不要写入文件

### 2. 不生成测试脚本
- ❌ 不要在 `scripts/` 目录创建测试脚本
- ❌ 不要创建独立的测试工具文件
- ✅ 可以通过现有的测试命令进行验证
- ✅ 可以使用临时命令测试（不保存为文件）

### 3. 问题排查方式
- ✅ **优先**：审查现有代码，分析问题根源
- ✅ **其次**：使用 `bash -c` 执行临时命令测试
- ✅ **允许**：使用 `/tmp` 目录的临时文件测试（自动清理）
- ❌ **禁止**：在项目中创建永久性的测试脚本文件

## 工作流程

### 问题修复流程
1. **阅读代码** - 使用 Read/Grep 工具审查相关代码
2. **分析问题** - 识别bug根本原因
3. **制定方案** - 规划修复步骤
4. **实施修复** - 直接修改源代码
5. **语法验证** - 使用 `bash -n` 验证语法
6. **口头总结** - 在响应中说明修复内容（不写文件）

### 临时测试（如必要）
```bash
# 使用临时文件测试（会自动清理）
bash -c "test command here"

# 或使用 /tmp 目录
cat > /tmp/test.sh << 'EOF'
...
EOF
bash /tmp/test.sh
rm /tmp/test.sh
```

## 文件创建规则

### ✅ 允许创建的文件
- 源代码文件（`.sh`, `.py`, `.js` 等）
- 配置文件（`.json`, `.yaml`, `.conf` 等）
- 必要的数据文件
- 项目功能所需的脚本

### ❌ 禁止创建的文件
- `claudedocs/*.md` - 任何文档、报告、指南
- `scripts/test_*.sh` - 测试脚本
- `scripts/debug_*.sh` - 调试脚本
- `*_summary.md` - 总结文档
- `*_guide.md` - 指南文档
- `*_report.md` - 报告文档

## 响应方式

### 修复完成后的响应格式
```
## 修复完成

发现的问题：
- 问题1描述
- 问题2描述

实施的修复：
- 修改文件A：具体改动
- 修改文件B：具体改动

验证结果：
- bash -n 语法检查通过
- 核心逻辑已修复

[不创建任何文档文件]
```

## 例外情况

### 唯一允许创建文档的情况
- 用户**明确要求**创建特定文档
- 项目**功能需要**的必要文档（如 README.md, API文档）

## 总结

**记住**：作为代码助手，专注于代码本身。通过口头交流总结，而不是创建文档文件。
