# 域名自动优选功能

## 更新时间
2025-10-12

## 功能概述

新增域名自动优选功能，在设置 Reality/TLS 协议的 SNI 伪装域名时，系统会自动测试高质量域名列表，并推荐延迟最低的域名。

---

## 功能特点

### 1. 智能测试
- **高质量域名库**: 预置10个高质量域名（微软、苹果、思科、英伟达等）
- **延迟测试**: 自动测试 TLS 握手延迟
- **DNS验证**: 验证域名 DNS 解析正常
- **实时显示**: 实时显示测试进度和结果

### 2. 灵活选择
- **自动推荐**: 默认推荐延迟最低的域名
- **列表选择**: 从前5名延迟最低的域名中选择
- **手动输入**: 支持手动输入自定义域名
- **格式验证**: 自动验证域名格式正确性

### 3. 用户友好
- **彩色显示**:
  - 绿色 (<200ms): 优秀，强烈推荐
  - 黄色 (200-500ms): 良好，可以使用
  - 红色 (>500ms): 较慢，不推荐
- **默认选择**: 按 Enter 直接使用推荐域名
- **容错处理**: 测试失败时自动使用默认域名

---

## 使用指南

### 方法一: 通过 SNI 域名管理

```bash
./xray-manager.sh
# → 6. 域名管理
# → 2. SNI 伪装域名
# → 1. 自动优选并设置 (推荐)
```

**交互流程**:
```
╔═══════════════════════════════════════╗
║    自动优选 SNI 伪装域名             ║
╚═══════════════════════════════════════╝

正在测试高质量域名...

  ✔ [ 1/10] www.microsoft.com                   156 ms
  ✔ [ 2/10] www.apple.com                       189 ms
  ✔ [ 3/10] www.cloudflare.com                  142 ms
  ✘ [ 4/10] www.bing.com                        超时
  ✔ [ 5/10] developer.apple.com                 203 ms
  ...

╔═══════════════════════════════════════╗
║        自动优选结果                  ║
╚═══════════════════════════════════════╝

推荐域名: www.cloudflare.com
延迟: 142ms

延迟最低的前 5 个域名：

序号  域名                                      延迟
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1     www.cloudflare.com                    142 ms
2     www.microsoft.com                     156 ms
3     www.apple.com                         189 ms
4     developer.apple.com                   203 ms
5     www.cisco.com                         218 ms

选择方式：
1. 自动使用推荐域名 (www.cloudflare.com)
2. 从上述列表中选择
3. 手动输入其他域名
0. 取消设置

请选择 [0-3, 默认: 1]:
```

**选择说明**:
- **选项 1**: 直接使用延迟最低的域名（默认，按Enter即可）
- **选项 2**: 从前5名中选择，输入序号1-5
- **选项 3**: 手动输入自定义域名（会验证格式）
- **选项 0**: 取消设置，保留当前域名

### 方法二: 完整域名测试（20+域名）

```bash
./xray-manager.sh
# → 6. 域名管理
# → 2. SNI 伪装域名
# → 5. 完整域名测试 (20+域名)
```

**说明**: 测试更多域名（20+），结果保存到 `recommended_domains.txt`

---

## 高质量域名列表

自动优选使用的高质量域名（CDN节点多、稳定性高）：

| 域名 | 服务商 | 特点 |
|------|--------|------|
| www.microsoft.com | 微软 | 全球CDN，稳定性极高 |
| www.apple.com | 苹果 | 全球分布，响应快速 |
| www.cloudflare.com | Cloudflare | 全球CDN，延迟极低 |
| www.bing.com | 微软 | 搜索服务，节点众多 |
| developer.apple.com | 苹果 | 开发者服务，稳定 |
| www.cisco.com | 思科 | 企业级网络，可靠 |
| www.intel.com | 英特尔 | 全球覆盖，稳定 |
| www.amd.com | AMD | 技术企业，可靠 |
| www.nvidia.com | 英伟达 | 全球CDN，快速 |
| login.microsoftonline.com | 微软 | 认证服务，高可用 |

---

## 技术实现

### 测试流程

```bash
# 1. 遍历高质量域名列表
for domain in "${premium_domains[@]}"; do
    # 2. 测试 TLS 握手延迟
    t1=$(date +%s%3N)
    openssl s_client -connect "$domain:443" -servername "$domain"
    t2=$(date +%s%3N)
    latency=$((t2 - t1))

    # 3. 验证 DNS 解析
    host "$domain"

    # 4. 保存成功结果
    echo "$latency $domain" >> temp_file
done

# 5. 按延迟排序
sort -n temp_file | head -n 5

# 6. 推荐延迟最低的域名
best_domain=$(sort -n temp_file | head -n 1 | awk '{print $2}')
```

### 核心函数

**文件**: `modules/domain.sh`

#### auto_select_sni_domain() - Line 616-758

自动优选并设置 SNI 域名的核心函数。

**功能**:
- 测试10个高质量域名的 TLS 握手延迟
- 显示前5名延迟最低的域名
- 提供3种设置方式（自动/选择/手动）
- 验证域名格式和可用性

**使用示例**:
```bash
# 在 SNI 域名管理中调用
manage_sni_domain() {
    case $choice in
        1) auto_select_sni_domain ;;  # 自动优选
    esac
}
```

#### manage_sni_domain() - Line 760-829

SNI 伪装域名管理菜单，新增自动优选选项。

**菜单结构**:
```
1. 自动优选并设置 (推荐)     ← 新增
2. 手动设置 SNI 域名
3. 查看推荐域名列表
4. 测试域名可用性
5. 完整域名测试 (20+域名)
0. 返回
```

---

## 使用场景

### 场景一: 新建 Reality 节点
```bash
1. 创建 Reality 节点
2. 进入域名管理 → SNI 伪装域名
3. 选择"自动优选并设置"
4. 系统自动测试并推荐最佳域名
5. 按 Enter 使用推荐域名
```

### 场景二: 优化现有配置
```bash
1. 节点连接不稳定
2. 进入域名管理 → SNI 伪装域名
3. 运行"自动优选并设置"
4. 从延迟最低的5个域名中选择
5. 重启节点应用新域名
```

### 场景三: 自定义域名验证
```bash
1. 有自己的伪装域名想法
2. 选择"自动优选并设置"
3. 查看测试结果作为参考
4. 选择"手动输入其他域名"
5. 输入自定义域名
```

---

## 延迟标准

| 延迟范围 | 等级 | 显示颜色 | 推荐度 |
|----------|------|----------|--------|
| < 200ms | 优秀 | 绿色 | ⭐⭐⭐⭐⭐ |
| 200-500ms | 良好 | 黄色 | ⭐⭐⭐⭐ |
| > 500ms | 较慢 | 红色 | ⭐⭐ |

**建议**:
- **优先选择绿色域名**: 延迟 < 200ms，连接快速稳定
- **黄色域名可用**: 延迟 200-500ms，可接受范围
- **避免红色域名**: 延迟 > 500ms，可能影响体验

---

## 错误处理

### 所有域名测试失败

**情况**: 网络问题或防火墙阻止连接

**处理**:
```bash
if [[ ! -s "$temp_file" ]]; then
    print_error "所有域名测试失败，使用默认域名"
    set_default_domain "www.microsoft.com"
    return 1
fi
```

**自动降级**: 使用 `www.microsoft.com` 作为默认域名

### 域名格式错误

**验证规则**:
```bash
if [[ "$custom_domain" =~ ^[a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?(\.[a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?)*$ ]]; then
    # 格式正确
else
    print_error "域名格式不正确"
fi
```

**合法格式**: `www.example.com`, `sub.domain.example.com`
**非法格式**: `-example.com`, `example-.com`, `example..com`

---

## 配置文件

### 默认域名存储

**文件**: `/usr/local/xray/data/default_domain.txt`

**内容**: 单行域名字符串
```
www.cloudflare.com
```

### 推荐域名列表

**文件**: `/usr/local/xray/data/recommended_domains.txt`

**格式**: 每行一个域名，按延迟排序
```
www.cloudflare.com
www.microsoft.com
www.apple.com
developer.apple.com
www.cisco.com
```

---

## 与其他功能集成

### Reality 节点创建

创建 Reality 节点时，自动使用当前默认域名：

```bash
# modules/node.sh
local sni=$(get_default_domain)

# 节点配置
"server_names": ["$sni"]
"dest": "$sni:443"
```

### Clash 订阅生成

Clash Meta 配置中使用 SNI 域名：

```yaml
# modules/subscription.sh
proxies:
  - name: "VLESS-Reality-443"
    type: vless
    servername: www.cloudflare.com  # 使用优选域名
    reality-opts:
      public-key: xxx
```

---

## 优势总结

### 1. 自动化
- ✅ 无需手动搜索和测试域名
- ✅ 自动筛选最佳选择
- ✅ 减少配置错误

### 2. 智能化
- ✅ 基于实际延迟测试推荐
- ✅ 综合考虑 TLS 握手和 DNS 解析
- ✅ 实时网络状况评估

### 3. 灵活性
- ✅ 支持自动、选择、手动三种模式
- ✅ 保留完整域名测试选项
- ✅ 允许自定义域名

### 4. 可靠性
- ✅ 高质量域名库（大厂CDN）
- ✅ 容错处理和默认降级
- ✅ 域名格式验证

---

## 最佳实践

### 首次配置
1. 运行自动优选测试
2. 选择延迟最低的域名
3. 测试节点连接
4. 确认稳定后保存

### 定期优化
1. 每月运行一次自动优选
2. 对比新旧域名延迟
3. 更换为更优域名（如有必要）

### 故障排查
1. 节点不稳定时，重新运行优选
2. 尝试列表中的其他域名
3. 避免使用延迟 > 500ms 的域名

---

## 后续优化计划

### 短期 (1-2周)
- [ ] 添加域名地理位置信息
- [ ] 支持自定义域名库
- [ ] 测试结果缓存（避免频繁测试）

### 中期 (1个月)
- [ ] 定时自动优选（每周/每月）
- [ ] 域名健康监控
- [ ] 失败域名自动切换

### 长期 (2-3个月)
- [ ] 机器学习推荐（基于历史数据）
- [ ] 区域化域名推荐（不同地区不同推荐）
- [ ] 域名质量评分系统

---

**功能完成时间**: 2025-10-12
**状态**: ✅ 已完成并测试
**影响范围**: SNI 域名管理模块
**向后兼容**: 完全兼容，新增功能不影响现有流程
